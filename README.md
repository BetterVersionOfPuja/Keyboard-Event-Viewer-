# Keyboard Event Viewer ⌨️

A simple JavaScript project that captures keyboard events and displays the pressed key, its keycode, and code in real time using DOM manipulation.

🔗 **Live Demo:**  
https://betterversionofpuja.github.io/Keyboard-Event-Viewer/

## Main Concepts Used
- Keyboard events (`keydown`)
- Event object (`e.key`, `e.code`, `e.keyCode`)
- DOM manipulation with `innerHTML`
- Template literals and conditional rendering

## Doubts Solved While Building
- How `innerHTML` renders HTML instead of printing code
(`innerHTML gets or sets the HTML content of an element; when set, the browser parses and renders the HTML string as actual elements.`)
- How event objects are passed to callback functions
- Difference between `innerHTML`, `innerText`, and `textContent`
- Handling special keys like the spacebar

## Tech Stack
- HTML
- CSS
- JavaScript
